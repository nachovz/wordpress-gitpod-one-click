image:
  file: Dockerfile
ports:
  - port: 8080
  - port: 3306
    onOpen: ignore
checkoutLocation: "lamp-example"
workspaceLocation: "."
tasks:
  - name: MySQL
    command: >
        mysqld --initialize-insecure;
        mysqld;
  - name: Apache
    command: >
        apachectl start;
  - name: WordPress
    command: >
        wp core download --path=$GITPOD_REPO_ROOT;
        touch $GITPOD_REPO_ROOT/.htaccess;
        chmod 660 $GITPOD_REPO_ROOT/.htaccess;
